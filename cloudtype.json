{
  "build": {
    "nodeVersion": "18",
    "buildCommand": "sh build.sh || (export NVM_DIR=\"$HOME/.nvm\" && [ -s \"$NVM_DIR/nvm.sh\" ] && \\. \"$NVM_DIR/nvm.sh\" && nvm install 18 && nvm use 18 && npm ci && npm run build) || npm run build",
    "installCommand": "npm ci",
    "outputDirectory": "dist"
  }
}

